Live Gemini settings checkpoint — provider may split a requested patch into multiple valid calls.
Target: final requested preference state + audit, then profile + appearance, all persisted across reload; do not require exactly one settings.update call.