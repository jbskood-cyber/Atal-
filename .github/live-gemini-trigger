Live Gemini local export checkpoint — execute provider certification now.
Target: natural export request → Gemini real → data.export_local → explicit sensitive confirmation → real browser download → backup JSON contains canonical Atal state; no EMPTY_MODEL_TURN.