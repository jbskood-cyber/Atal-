Block 9 — Final Polish & Exhaustive Agent QA — CLEAN FINAL current-head recertification after H-051/H-052/H-053.
Trigger exactly one provider-preflight-gated NO-SKIPPED Gemini + Chromium checkpoint from feature/atal-final-polish-agent-qa. Current product checkpoint before trigger: 68e51f539511f551c2f043a11671f5c83d88dd11; quality #1030 PASS, Behavior #601 PASS, E2E #1006 PASS. H-051: explicit natural 'última sesión completada' uses a semantic session reference resolved only inside selected patient+plan context to the most recent completed session; arbitrary explicit session IDs remain strict. H-052: contextual user turn is durable before provider latency, processing-state persistence cannot truncate it, close is disabled during processing and deferred after settlement; final assistant/user turn must survive close/reopen/reload without cross-patient contamination. H-053: lifecycle harness waits for final narration only after canonical paused state + event are confirmed. Require exhaustive natural plan create/edit/add/remove/replace/lifecycle/readback/reload, all five contextual surfaces + A/B/global isolation, grounded reads/non-hallucination, compact text-first mobile UI, field-pure writes, and the full physiotherapist chain patient/record → plan → exercises → activation → guided session → exact completion metrics → session summary/read → report review → patient edit → Undo → hard reload. A retry is not automatically accepted: classify failed first attempts from canonical store/audit evidence. Do not weaken Action Core, confirmations, requiredTools, duplicate protection, entity disambiguation, field hygiene, or grounding to force green.