Adversarial autonomous QA checkpoint — H-024 recovery verification v4.
Target: run the real Gemini + Chromium suite under the synthetic opaque AI Studio GEMINI_MODEL environment after: (1) underspecified treatment requests are read-only and clarify before mutation; (2) grounded reads must return canonical values; (3) undeclared function calls are safe-retry failures across the model cascade; (4) tool-shaped JSON emitted as plain text is not shown as a successful answer: the agent requests one repair, forces a declared function call, then executes only the canonical allowed tool; (5) Undo verifies restore + reload. This checkpoint must not be gate-skipped.
