Live Gemini contextual isolation checkpoint — two patient contexts plus global assistant.
Target: Patient A contextual note -> canonical state A only; Patient B contextual note -> canonical state B only; distinct contextual conversations; global history remains isolated; reload preserves all state.