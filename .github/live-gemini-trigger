Block 9 — Final Polish & Exhaustive Agent QA — CLEAN recertification after complete H-051 grounding.
Trigger exactly one provider-preflight-gated NO-SKIPPED Gemini + Chromium checkpoint from feature/atal-final-polish-agent-qa. Current product checkpoint before trigger: cf1d2333d2321b1e917d6a62629835422f5f05ce; quality #1008 PASS, Behavior #579 PASS, E2E #984 PASS. H-051 now covers both canonical session-read routes observed in the real physiotherapist flow: app.read sessions and session.summarize_recent. When either same-turn successful result contains exactly one canonical session, report.review is grounded to that session ID before execution; with zero or multiple sessions Atal does not guess. Preserve H-046 field hygiene, H-047 guided-session navigation, H-048/H-049 membership grounding, H-050 exercise-only stale-ID exact-label fallback, opaque AI Studio GEMINI_MODEL simulation and provider preflight. Require a clean exhaustive natural plan matrix plus the complete physiotherapist chain patient/record → plan → exercises → activation → guided session → exact metrics → session read/summary → report review → patient edit → Undo → hard reload. A product/model flake in that chain is not accepted as final just because retry passes; classify the failed attempt from canonical store evidence. Do not weaken Action Core, confirmations, requiredTools, duplicate protection, entity disambiguation, or grounding to force green.