AI Studio invalid GEMINI_MODEL recovery checkpoint — sanitize opaque platform overrides.
Target: execute the real Gemini browser regression on the latest branch state after modelFallback rejects non-Gemini environment values, then retain fresh conversation, patient-create persistence/readback, session, contextual isolation, export and delivery coverage. This checkpoint must not be gate-skipped.
