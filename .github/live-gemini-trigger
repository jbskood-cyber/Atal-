Live Gemini settings checkpoint — request-scoped preference schema + idle composer sequencing.
Target: combined preference request requires exactly the canonical requested keys at provider schema level; then profile + appearance persist across reload.
