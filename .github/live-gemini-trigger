Live Gemini local export checkpoint — real provider plus browser download.
Target: export_data → data.export_local → sensitive confirmation → client download effect → backup JSON contains canonical Atal state.
