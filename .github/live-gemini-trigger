Live Gemini local export checkpoint — canonical export contract repaired and tool selection narrowed.
Target: export_data → data.export_local only → sensitive confirmation → real client download → backup JSON contains canonical Atal state.
