Adversarial autonomous QA checkpoint — H-024 recovery verification v3.
Target: run the real Gemini + Chromium suite under the synthetic opaque AI Studio GEMINI_MODEL environment after: (1) underspecified treatment requests are read-only and clarify before mutation; (2) grounded reads must return canonical values; (3) undeclared Gemini function names such as get_patients/patient.list are rejected and treated as retryable model-output failures so the cascade can recover without aliasing unsafe capabilities; (4) Undo locator verifies real restore + reload. This checkpoint must not be gate-skipped.
