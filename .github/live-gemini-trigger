Block 9 — Final Polish & Exhaustive Agent QA — FINAL current-head real-Gemini recertification after execution-boundary field hygiene.
Trigger exactly one provider-preflight-gated NO-SKIPPED Gemini + Chromium checkpoint from feature/atal-final-polish-agent-qa. Product checkpoint before trigger: cc060e9ea3271d92bbf35b7382d81fc5091d2a29; quality #987 PASS, Behavior #558 PASS, E2E #963 PASS. H-046 adds execution-boundary normalization for obvious narrative wrappers in structured writes after verified RED a194e6c (quality #984 / Behavior #555 failed as expected). Preserve opaque AI Studio GEMINI_MODEL simulation and abort before browser matrix if provider preflight is unhealthy. Certify field-pure patient/record/plan/exercise/profile writes, exhaustive natural plan create/edit/membership/replacement/lifecycle/ambiguity/readback/reload, contextual isolation, no-alucinación, compact text-first UI, and the complete physiotherapist chain patient → clinical record → plan → exercises → activation confirmation → guided session → exact completion metrics → report review → patient edit → Undo → hard reload. Treat provider/model flakes separately only when retry succeeds and canonical store/audit evidence proves no doubtful mutation. Stable product REDs become TDD; never weaken Action Core guards, confirmation semantics, requiredTools, duplicate protection, entity disambiguation, or the new structured-write hygiene boundary to force green.