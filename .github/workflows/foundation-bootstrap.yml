name: foundation-bootstrap
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic runs disabled while the branch is completed."
