name: foundation-bootstrap
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Foundation checks are paused while the branch is completed."
