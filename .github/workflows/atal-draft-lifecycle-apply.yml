name: apply-atal-draft-lifecycle-disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary draft lifecycle workflow disabled after applying the patch."
