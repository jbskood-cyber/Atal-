name: apply-atal-patients-read-disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary patients read workflow disabled after applying the capability."
