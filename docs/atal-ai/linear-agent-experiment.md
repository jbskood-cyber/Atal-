# Atal IA — Linear-style agent experiment

This branch is an isolated implementation line for rebuilding Atal IA as a natural, persistent, tool-using agent.

Canonical base SHA: `f12fadede8fe1badd5ae16e64d1fdf2b2cc676d9`

Protections:

- `main` is untouched.
- PR #18 and its source branch remain untouched.
- No merge or ready-for-review transition is authorized.
- The experiment preserves the deterministic Atal core, audit, confirmation, and Undo.
