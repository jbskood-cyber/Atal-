# Configura estas variables como secretos del proyecto en Google AI Studio.
# Nunca uses el prefijo VITE_ para la clave: debe permanecer únicamente en el servidor.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash
