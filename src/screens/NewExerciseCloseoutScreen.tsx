'use client';
import{FormEvent,useEffect,useState}from'react';import{useRouter}from'next/navigation';import{ArrowLeft,Check,Clock3,ImagePlus,Repeat2,Video}from'lucide-react';import{AtalShell}from'@/src/components/atal/AtalShell';import{createLocalExercise,deleteExercise,updateLocalExercise,type ExerciseMedia}from'@/src/data/localExercises';import{saveExerciseMedia}from'@/src/data/exerciseMediaRepository';import{validateExerciseInput}from'@/src/domain/validation';
type ExecutionMode='repetitions'|'time'|'both';type MediaKind=ExerciseMedia['type'];
export function NewExerciseCloseoutScreen(){const router=useRouter(),[name,setName]=useState(''),[instructions,setInstructions]=useState(''),[mode,setMode]=useState<ExecutionMode>('repetitions'),[media,setMedia]=useState<MediaKind>('none'),[saved,setSaved]=useState(false),[saving,setSaving]=useState(false),[files,setFiles]=useState<File[]>([]),[error,setError]=useState(''),[preview,setPreview]=useState(''),[sets,setSets]=useState('3'),[repetitions,setRepetitions]=useState('10'),[maxPain,setMaxPain]=useState('3');useEffect(()=>{if(!files[0]){setPreview('');return}const url=URL.createObjectURL(files[0]);setPreview(url);return()=>URL.revokeObjectURL(url)},[files]);const canSave=Boolean(name.trim()&&instructions.trim()&&!saving&&!saved),submit=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();if(!canSave)return;setSaving(true);const form=new FormData(event.currentTarget),text=(key:string)=>String(form.get(key)??'').trim(),number=(key:string,fallback:number)=>{const parsed=Number(form.get(key));return Number.isFinite(parsed)?parsed:fallback},instructionList=instructions.split(/\r?\n/).map(line=>line.trim()).filter(Boolean),setsValue=number('sets',0),repetitionValue=mode==='time'?undefined:number('repetitions',0),timeValue=mode==='repetitions'?undefined:text('time'),maxPainValue=number('maxPain',3),validation=validateExerciseInput({name,instructions:instructionList,sets:setsValue,repetitions:repetitionValue,time:timeValue,maxPain:maxPainValue});if(!validation.valid){setError(Object.values(validation.errors)[0]);setSaving(false);return}if(media!=='none'&&!files.length){setError('Selecciona el recurso visual antes de guardar.');setSaving(false);return}let exercise:ReturnType<typeof createLocalExercise>|null=null;try{exercise=createLocalExercise({name:name.trim(),region:text('region'),category:text('category'),objective:text('objective'),startingPosition:text('startingPosition'),instructions:instructionList,precautions:text('precautions'),equipment:text('equipment'),difficulty:text('difficulty'),sets:setsValue,repetitions:repetitionValue,time:timeValue,rest:text('rest'),maxPain:maxPainValue,tags:text('tags').split(',').map(tag=>tag.trim()).filter(Boolean),notes:text('notes'),media:{type:'none'}});if(media!=='none'){const record=await saveExerciseMedia(exercise.id,media==='video'?'video':media==='sequence'?'sequence':'image',files);updateLocalExercise(exercise.id,{media:{type:media,mediaId:record.id}})}setSaved(true);setSaving(false);window.setTimeout(()=>router.push(`/exercises/${exercise?.id}`),450)}catch(problem){if(exercise)try{deleteExercise(exercise.id)}catch{}setError(problem instanceof Error?problem.message:'No pudimos guardar el ejercicio.');setSaving(false)}};return <AtalShell><main className="atal-content atal-flow-page atal-new-exercise-page"><div className="atal-flow-topbar"><button type="button" aria-label="Volver" onClick={()=>router.back()}><ArrowLeft/></button><span>Crear ejercicio</span><i/></div><div className="atal-form-heading atal-form-heading--supporting"><p>Construye una guía personalizada para tu biblioteca local.</p></div><form className="atal-clinical-form atal-exercise-form atal-exercise-form--closed" onSubmit={submit}><fieldset className="atal-exercise-section"><legend>Información clínica</legend><div className="atal-field-grid is-balanced"><label className="atal-field"><span>Nombre</span><input required name="name" value={name} onChange={e=>setName(e.target.value)} placeholder="Ej. Marcha lateral con banda"/></label><label className="atal-field"><span>Región corporal</span><input name="region" placeholder="Escribe cualquier región"/></label></div><div className="atal-field-grid is-balanced"><label className="atal-field"><span>Categoría</span><input name="category" placeholder="Ej. control motor"/></label><label className="atal-field"><span>Objetivo</span><input name="objective" placeholder="Ej. mejorar estabilidad"/></label></div><label className="atal-field"><span>Posición inicial</span><textarea name="startingPosition" placeholder="Describe cómo debe colocarse el paciente…"/></label><label className="atal-field"><span>Instrucciones</span><textarea required name="instructions" value={instructions} onChange={e=>setInstructions(e.target.value)} placeholder="Un paso por línea. Ej. Mantén el tronco estable…"/></label><label className="atal-field"><span>Precauciones</span><textarea name="precautions" placeholder="Señales para detener o adaptar el ejercicio…"/></label></fieldset><fieldset className="atal-exercise-section"><legend>Prescripción</legend><div className="atal-mode-switch" role="group" aria-label="Tipo de ejercicio"><button type="button" className={mode==='repetitions'?'is-active':''} onClick={()=>setMode('repetitions')}><Repeat2/> Repeticiones</button><button type="button" className={mode==='time'?'is-active':''} onClick={()=>setMode('time')}><Clock3/> Tiempo</button><button type="button" className={mode==='both'?'is-active':''} onClick={()=>setMode('both')}>Ambos</button></div><div className="atal-prescription-fields"><label className="atal-field"><span>Series</span><input name="sets" inputMode="numeric" type="number" min="1" value={sets} onChange={e=>setSets(e.target.value)}/></label>{mode!=='time'&&<label className="atal-field"><span>Repeticiones</span><input name="repetitions" inputMode="numeric" type="number" min="1" value={repetitions} onChange={e=>setRepetitions(e.target.value)}/></label>}{mode!=='repetitions'&&<label className="atal-field"><span>Tiempo</span><input name="time" placeholder="Ej. 45 segundos"/></label>}<label className="atal-field"><span>Descanso</span><input name="rest" placeholder="Ej. 30 segundos"/></label><label className="atal-field"><span>Dolor máximo permitido</span><input name="maxPain" inputMode="numeric" type="number" min="0" max="10" value={maxPain} onChange={e=>setMaxPain(e.target.value)}/></label></div><div className="atal-field-grid is-balanced"><label className="atal-field"><span>Equipo o material</span><input name="equipment" placeholder="Ej. banda elástica o ninguno"/></label><label className="atal-field"><span>Dificultad</span><input name="difficulty" placeholder="Escribe el nivel o adaptación"/></label></div></fieldset><fieldset className="atal-exercise-section"><legend>Organización y recurso visual</legend><label className="atal-field"><span>Etiquetas</span><input name="tags" placeholder="Separadas por comas; acepta texto libre"/></label><label className="atal-field"><span>Observaciones</span><textarea name="notes" placeholder="Indicaciones adicionales para el fisioterapeuta…"/></label><div className="atal-media-picker" role="group" aria-label="Recurso visual local">{([{id:'image',label:'Imagen',icon:<ImagePlus/>},{id:'video',label:'Video',icon:<Video/>},{id:'sequence',label:'Secuencia',icon:<ImagePlus/>},{id:'none',label:'Sin recurso',icon:<Check/>}]as const).map(item=><button type="button" key={item.id} className={media===item.id?'is-active':''} onClick={()=>{setMedia(item.id);setFiles([]);setError('')}}>{item.icon}<span>{item.label}</span></button>)}</div>{media!=='none'&&<label className="atal-exercise-media"><ImagePlus/><span><b>{files.length?`${files.length} archivo(s) seleccionado(s)`:'Seleccionar recurso local'}</b><small>{media==='video'?'Video corto, máximo 25 MB':media==='sequence'?'Hasta 12 imágenes de 8 MB':'Imagen de hasta 8 MB'}</small></span><input type="file" accept={media==='video'?'video/*':'image/*'} multiple={media==='sequence'} onChange={e=>{setFiles(Array.from(e.target.files??[]));setError('')}}/></label>}{preview&&<div className="atal-media-preview">{media==='video'?<video src={preview} controls/>:<img src={preview} alt="Vista previa del recurso"/>}<button type="button" onClick={()=>setFiles([])}>Eliminar recurso</button></div>}</fieldset>{error&&<p className="atal-form-error" role="alert">{error}</p>}<button type="submit" className="atal-submit-button" disabled={!canSave} aria-busy={saving}><Check/>{saved?'Ejercicio guardado':saving?'Guardando…':canSave?'Guardar ejercicio local':'Completa los campos obligatorios'}</button>{!canSave&&!saving&&!saved&&<p className="atal-form-hint">Completa nombre e instrucciones para guardar.</p>}</form></main></AtalShell>}
