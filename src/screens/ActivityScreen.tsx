'use client';
import { useMemo,useState } from 'react';import { useSearchParams,useRouter } from 'next/navigation';import { Activity,CalendarDays,ChevronRight,ClipboardCheck,FileText,Filter,Search,TrendingUp } from 'lucide-react';import { AtalShell } from '@/src/components/atal/AtalShell';import { Avatar } from '@/src/components/atal/Avatar';import { useAtalStore } from '@/src/data/atalStore';
type View='tracking'|'reports';
export function ActivityScreen(){const params=useSearchParams();const router=useRouter();const[view,setView]=useState<View>(params.get('view')==='reports'?'reports':'tracking');const[query,setQuery]=useState('');const[pendingOnly,setPendingOnly]=useState(false);const state=useAtalStore((store)=>({sessions:store.sessions,patients:store.patients,plans:store.plans}));const entries=useMemo(()=>state.sessions.filter((session)=>{const patient=state.patients.find((item)=>item.id===session.patientId);const plan=state.plans.find((item)=>item.id===session.planId);const text=`${patient?.name} ${patient?.diagnosis} ${plan?.title}`.toLowerCase();return(!pendingOnly||!session.reviewedAt)&&text.includes(query.toLowerCase())&&(!params.get('patientId')||session.patientId===params.get('patientId'));}),[state,query,pendingOnly,params]);const completed=state.sessions.filter((item)=>item.status==='completed').length;const adherence=state.sessions.length?Math.round(completed/state.sessions.length*100):null;const pending=state.sessions.filter((item)=>!item.reviewedAt).length;const changeView=(next:View)=>{setView(next);router.replace(`/activity?view=${next}`,{scroll:false});};
return <AtalShell><main className="atal-content atal-list-page atal-activity-page"><div className="atal-page-heading"><div><span className="atal-eyebrow">Clínica</span><h1>Actividad</h1></div></div><div className="atal-view-switch"><button type="button" className={view==='tracking'?'is-active':''} onClick={()=>changeView('tracking')}><Activity/>Seguimiento</button><button type="button" className={view==='reports'?'is-active':''} onClick={()=>changeView('reports')}><FileText/>Reportes</button></div><label className="atal-premium-search"><Search/><input value={query} onChange={(event)=>setQuery(event.target.value)} placeholder="Buscar paciente o plan"/><button type="button" className={pendingOnly?'is-active':''} aria-label="Mostrar solo pendientes" onClick={()=>setPendingOnly((value)=>!value)}><Filter/></button></label><section className="atal-activity-summary"><article><span><TrendingUp/></span><div><small>Adherencia</small><strong>{adherence===null?'—':`${adherence}%`}</strong><em>{state.sessions.length?`${state.sessions.length} sesiones`:'Sin sesiones'}</em></div></article><article><span className="is-warm"><ClipboardCheck/></span><div><small>Por revisar</small><strong>{pending}</strong><em>{pending===1?'1 reporte':`${pending} reportes`}</em></div></article></section><div className="atal-list-meta"><b>{view==='tracking'?'Sesiones recientes':'Reportes clínicos'}</b><span>{entries.length}</span></div><div className="atal-activity-feed">{entries.map((session)=>{const patient=state.patients.find((item)=>item.id===session.patientId);const plan=state.plans.find((item)=>item.id===session.planId);return <button type="button" key={session.id} onClick={()=>router.push(`/activity/${session.id}`)}><Avatar name={patient?.name??'Paciente'}/><span><b>{patient?.name??'Paciente no disponible'}</b><small>{plan?.title??'Plan no disponible'} · {session.status==='completed'?'Completada':'Parcial'}</small><em>Dolor {session.startPain}/10 → {session.endPain}/10 · {session.reviewedAt?'Revisado':'Pendiente'}</em></span><time><CalendarDays/>{new Date(session.completedAt).toLocaleDateString('es-MX')}</time><i className={session.endPain>=7?'is-attention':''}/><ChevronRight/></button>})}</div>{!entries.length&&<div className="atal-empty"><Activity/><p>Sin actividad</p></div>}</main></AtalShell>}
