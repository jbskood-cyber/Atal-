'use client';
import { FormEvent,useState } from 'react';import { ArrowLeft,Check,Download,ImagePlus,MessageSquareText,Send } from 'lucide-react';import { useRouter } from 'next/navigation';import { AtalShell } from '@/src/components/atal/AtalShell';import { AppSelect } from '@/src/components/atal/AppSelect';import { addFeedback,updateFeedbackStatus,useAtalStore } from '@/src/data/atalStore';
const categories=['Reportar un error','Proponer una mejora','Solicitar una función','Diseño o nuevo tema','Otro comentario'];
function buildText(input:{category:string;title:string;description:string;screen:string;email:string;contactAllowed:boolean}){return `Comentario de Atal\n\nCategoría: ${input.category}\nTítulo: ${input.title}\nDescripción: ${input.description}\nPantalla: ${input.screen}\nVersión: 0.1.0\nDispositivo: ${navigator.userAgent}\nCorreo opcional: ${input.email||'No proporcionado'}\nAutoriza contacto: ${input.contactAllowed?'Sí':'No'}`;}
function downloadText(name:string,text:string){const blob=new Blob([text],{type:'text/plain;charset=utf-8'});const url=URL.createObjectURL(blob);const anchor=document.createElement('a');anchor.href=url;anchor.download=name;anchor.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
export function FeedbackScreen(){const router=useRouter();const history=useAtalStore((state)=>state.feedback);const[category,setCategory]=useState(categories[0]);const[title,setTitle]=useState('');const[description,setDescription]=useState('');const[email,setEmail]=useState('');const[contactAllowed,setContactAllowed]=useState(false);const[file,setFile]=useState<File|null>(null);const[message,setMessage]=useState('');const submit=async(event:FormEvent)=>{event.preventDefault();if(!title.trim()||!description.trim())return;const screen=location.pathname;const item=addFeedback({category,title:title.trim(),description:description.trim(),screen,appVersion:'0.1.0',device:navigator.userAgent,email:email.trim(),contactAllowed,screenshotName:file?.name,status:'prepared'});const text=buildText({category,title,description,screen,email,contactAllowed});try{if(navigator.share&&(!file||!navigator.canShare||navigator.canShare({files:[file]}))){await navigator.share({title:`Atal: ${title}`,text,files:file?[file]:undefined});updateFeedbackStatus(item.id,'shared');setMessage('Comentario compartido y copia local guardada.');}else if(navigator.clipboard){await navigator.clipboard.writeText(text);updateFeedbackStatus(item.id,'copied');downloadText(`atal-comentario-${item.id}.txt`,text);setMessage('Comentario copiado y archivo descargado.');}else{downloadText(`atal-comentario-${item.id}.txt`,text);updateFeedbackStatus(item.id,'downloaded');setMessage('Archivo de comentario descargado.');}setTitle('');setDescription('');setFile(null);}catch{downloadText(`atal-comentario-${item.id}.txt`,text);updateFeedbackStatus(item.id,'downloaded');setMessage('No se pudo compartir; descargamos una copia para que no pierdas el comentario.');}};return <AtalShell><main className="atal-content atal-flow-page"><div className="atal-flow-topbar"><button type="button" onClick={()=>router.back()}><ArrowLeft/></button><span>Ayuda y comentarios</span><i/></div><div className="atal-form-heading"><h1>Cuéntanos qué necesitas</h1><p>Atal guarda una copia local y utiliza las opciones de compartir de tu dispositivo.</p></div><form className="atal-clinical-form" onSubmit={submit}><fieldset><label className="atal-field"><span>Categoría</span><AppSelect label="Categoría del comentario" value={category} options={categories} onChange={setCategory}/></label><label className="atal-field"><span>Título</span><input required value={title} onChange={(event)=>setTitle(event.target.value)} placeholder="Resumen breve"/></label><label className="atal-field"><span>Descripción</span><textarea required value={description} onChange={(event)=>setDescription(event.target.value)} placeholder="Explica qué pasó o qué te gustaría mejorar…"/></label><label className="atal-field"><span>Correo opcional</span><input type="email" value={email} onChange={(event)=>setEmail(event.target.value)}/></label><label className="atal-feedback-file"><ImagePlus/><span><b>{file?.name??'Adjuntar captura opcional'}</b><small>Imagen de hasta 5 MB</small></span><input type="file" accept="image/*" onChange={(event)=>{const selected=event.target.files?.[0];if(selected&&selected.size<=5*1024*1024)setFile(selected);else if(selected)setMessage('La captura supera 5 MB.');}}/></label><label className="atal-feedback-consent"><input type="checkbox" checked={contactAllowed} onChange={(event)=>setContactAllowed(event.target.checked)}/><span>Autorizo que me contacten si posteriormente se conecta un canal remoto.</span></label></fieldset><button type="submit" className="atal-submit-button" disabled={!title.trim()||!description.trim()}><Send/>Preparar y compartir</button></form>{message&&<p className="atal-action-message" role="status"><Check/>{message}</p>}<section className="atal-profile-section"><h2>Historial local</h2><div className="atal-note-history">{history.map((item)=><article key={item.id}><MessageSquareText/><div><b>{item.title}</b><p>{item.category} · {item.status}</p><small>{new Date(item.createdAt).toLocaleString('es-MX')}</small></div><button type="button" onClick={()=>downloadText(`atal-comentario-${item.id}.txt`,buildText(item))}><Download/>Descargar</button></article>)}{!history.length&&<p>No hay comentarios preparados todavía.</p>}</div></section></main></AtalShell>}
