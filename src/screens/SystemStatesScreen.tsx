'use client';import { useState } from 'react';import { AlertTriangle, CheckCircle2, CloudOff, Inbox, LoaderCircle, RotateCcw } from 'lucide-react';import { AtalShell } from '@/src/components/atal/AtalShell';
const states={empty:{icon:Inbox,title:'Sin resultados',text:'Prueba modificando tu búsqueda o creando un registro.'},loading:{icon:LoaderCircle,title:'Cargando información',text:'Estamos preparando tu espacio clínico.'},error:{icon:AlertTriangle,title:'Algo salió mal',text:'No pudimos cargar esta información.'},offline:{icon:CloudOff,title:'Sin conexión',text:'Revisa tu conexión e inténtalo nuevamente.'},success:{icon:CheckCircle2,title:'Todo listo',text:'Los cambios se guardaron correctamente.'}} as const;
export function SystemStatesScreen(){const[current,setCurrent]=useState<keyof typeof states>('empty');const state=states[current];const Icon=state.icon;return <AtalShell><main className="atal-content atal-flow-page"><div className="atal-form-heading"><span className="atal-eyebrow">Sistema visual</span><h1>Estados de la app</h1><p>Comportamientos locales antes de conectar servicios.</p></div><div className="atal-state-tabs">{Object.keys(states).map(key=><button key={key} className={current===key?'is-active':''} onClick={()=>setCurrent(key as keyof typeof states)}>{key}</button>)}</div><section className={`atal-system-state is-${current}`}><span><Icon className={current==='loading'?'is-spinning':''}/></span><h2>{state.title}</h2><p>{state.text}</p><button><RotateCcw/>Reintentar</button></section></main></AtalShell>}
